{
  "name": "mermaid-wizard",
  "version": "1.0.0",
  "description": "A modern, responsive web application for creating flowcharts using Mermaid.js",
  "scripts": {
    "start": "http-server -c-1",
    "build": "npm run clean && npm run copy-all",
    "clean": "rimraf dist",
    "copy-all": "npm run create-dirs && npm run copy-files",
    "create-dirs": "mkdir -p dist/assets dist/js dist/lib",
    "copy-files": "cpx \"index.html\" dist/ && cpx \".htaccess\" dist/ && cpx \"favicon.ico\" dist/ && cpx -r assets/** dist/assets && cpx -r js/** dist/js && cpx -r lib/** dist/lib"
  },
  "dependencies": {
    "html-to-image": "^1.11.11",
    "mermaid": "^10.8.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "http-server": "^14.1.1",
    "rimraf": "^5.0.10"
  }
}
